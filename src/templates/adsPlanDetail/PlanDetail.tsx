import * as React from 'react';

// @ts-ignore
const PlanDetail = ({ campaign }) => {

    const MyDocument = () => {
        return (
            <h1>Teste</h1>
        );
    }
    return (<MyDocument/>)
};

export default PlanDetail;